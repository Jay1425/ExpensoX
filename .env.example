# Flask secret key used for session signing and CSRF protection
SECRET_KEY=change-me

# Database connection string (SQLite for local dev by default)
DATABASE_URL=sqlite:///expensox.db

# OTP settings
OTP_EXPIRY_MINUTES=5
DEFAULT_CURRENCY=USD

# Optional SMTP settings (leave blank to fall back to console logging)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=aivisionaries.teams@gmail.com
SMTP_PASSWORD=rves fkcw ikpq mbmw
SMTP_USE_TLS=true
SMTP_FROM_EMAIL=ExpensoX aivisionaries.teams@gmail.com
